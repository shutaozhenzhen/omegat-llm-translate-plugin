name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Setup Gradle 6.9
        run: |
          curl -sL -o /tmp/gradle.zip \
            "https://services.gradle.org/distributions/gradle-6.9-bin.zip"
          unzip -q /tmp/gradle.zip -d /tmp/gradle
          echo "/tmp/gradle/gradle-6.9/bin" >> $GITHUB_PATH

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Download OmegaT JAR
        run: |
          mkdir -p ../omegat/build/libs
          curl -sL -o /tmp/omegat.zip \
            "https://sourceforge.net/projects/omegat/files/OmegaT%20-%20Standard/OmegaT%206.0.1/OmegaT_6.0.1_Without_JRE.zip/download"
          unzip -j /tmp/omegat.zip "*/OmegaT.jar" -d ../omegat/build/libs/

      - name: Build with Gradle
        run: gradle fatJar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: omegat-llm-translate-plugin
          path: build/libs/*.jar
